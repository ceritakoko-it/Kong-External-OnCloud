_format_version: "3.0"
_konnect:
  control_plane_name: __CONTROL_PLANE_NAME__
services:
- connect_timeout: 60000
  enabled: true
  host: __POKEMON_SERVICE_HOST__
  name: pokemon-service
  plugins:
  - config:
      custom_fields_by_lua: {}
      path: /usr/local/kong/logs/transaction.log
      reopen: false
    enabled: true
    name: file-log
    protocols:
    - grpc
    - grpcs
    - http
    - https
  port: 443
  protocol: https
  read_timeout: 60000
  retries: 5
  write_timeout: 60000
